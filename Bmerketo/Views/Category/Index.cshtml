@model IEnumerable<Bmerketo.Models.Entities.CategoryEntity>


@{
    ViewData["Title"] = "Category";
}

<div class="row">
    <div class="col-md-8 offset-md-2">

        <table class="table">
            <thead>
                <tr class="text-center">
                    <th>@Html.DisplayNameFor(model => model.CategoryName)</th>

                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>

                        <td class="align-middle">
                            @Html.DisplayFor(modelItem => item.CategoryName)
                        </td>

                        @* @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                    {*@
                        <td class="align-middle">
                            <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id"><i class="fa-solid fa-file-pen"></i> Edit </a>   
                            <a class="btn btn-danger text-white" asp-action="Delete" asp-route-id="@item.Id"><i class="fa-solid fa-trash"></i> Delete </a>
                        </td>
                        @* }*@

                    </tr>
                }
            </tbody>

        </table>

    </div>
    
</div>
<div class="text-white" style="position:  fixed; right:25px; bottom: 25px;">
    <a class="btn btn-success text-white" asp-controller="Category" asp-action="Create"><i class="fa-regular fa-plus"></i> New </a>
</div>
@*@await Html.PartialAsync("_CreateItem", "BrandEntity") *@



<div id="partialzoon">
</div>
@section Scripts{
    <script>
        $('#partialzoon').load("/Category/CategoryPartialView")
    </script>
}